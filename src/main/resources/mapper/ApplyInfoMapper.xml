<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.imagehandle2022.seal.mapper.ApplyInfoMapper">

    <resultMap id="BaseResultMap" type="com.example.imagehandle2022.entity.SealApplyInfo">
        <result column="applyId" jdbcType="VARCHAR" property="applyId" />
        <result column="apply_person_id" jdbcType="VARCHAR" property="apply_person_id" />
        <result column="institutionCode" jdbcType="VARCHAR" property="institutionCode" />
        <result column="sealName" jdbcType="VARCHAR" property="sealName" />
        <result column="sealCode" jdbcType="VARCHAR" property="sealCode" />
        <result column="applyDate" jdbcType="VARCHAR" property="applyDate" />
    </resultMap>

    <insert id="insertSealApplyInfo" parameterType="SealApplyInfo">
        insert into seal_apply_info (applyId,apply_person_id,institutionCode,sealName,sealCode,applyDate)
            value(
            #{applyId},
            #{applyPersonId},
            #{institutionCode},
            #{sealName},
            #{sealCode},
            NOW()
            )
    </insert>

    <select id="querySealApplyInfo" resultType="SealApplyInfo">
        select * from seal_apply_info where 1=1
        <if test="applyId != null and applyId != ''">
            and applyId =  #{applyId}
        </if>
        <if test="apply_person_id != null and apply_person_id != ''">
            and apply_person_id =  #{apply_person_id}
        </if>
        <if test="institutionCode != null and institutionCode != ''">
            and institutionCode =  #{institutionCode}
        </if>
        <if test="sealName != null and sealName != ''">
            and sealName =  #{sealName}
        </if>
        <if test="sealCode != null and sealCode != ''">
            and sealCode =  #{sealCode}
        </if>
    </select>

</mapper>